# Manifest Placeholder

Reserved for future manifest work.

Phase 1A intentionally does not add a functional `manifest.json`, browser permissions, host permissions, content scripts, `activeTab`, `scripting`, `tabs`, `webRequest`, storage, or OpenFront host access.
